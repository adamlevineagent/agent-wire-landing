{
	# Hardcoded :8080 (not $PORT). Temps injects PORT based on its own UI
	# field, but separately detects the container's EXPOSE for routing —
	# which means reading $PORT can produce a mismatch. Pinning the listen
	# port guarantees Caddy speaks on exactly the port we EXPOSE.
	auto_https off
}

:8080 {
	root * /usr/share/caddy
	encode zstd gzip
	file_server
	# Long cache for the font preconnect; short cache for the HTML itself
	# so content updates are visible without a forced reload.
	@html path *.html /
	header @html Cache-Control "public, max-age=60, must-revalidate"
	header /* Cache-Control "public, max-age=300"
	# Basic security headers. CSP is permissive enough for inline styles
	# (the page uses inline <style> and <script>) and Google Fonts.
	header {
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
		Permissions-Policy "interest-cohort=()"
	}
}
